{
  "name": "notiflow",
  "version": "1.0.0",
  "private": true,
  "description": "NotiFlow - Medical supply order notification system",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web",
    "lint:web": "npm run lint --workspace=apps/web"
  }
}
